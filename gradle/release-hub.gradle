apply plugin: "com.dipien.releaseshub.gradle.plugin"

releasesHub {
    dependenciesBasePath = "gradle"
    dependenciesClassNames = ["dependencies.gradle"]
    pullRequestEnabled = true
    pullRequestLabels = ["dependencies"]
//    pullRequestReviewers = ["diklimchuk"]
    headBranchPrefix = "update_dependencies/"
    baseBranch = "main"
    gitHubUserEmail = "klimchuk.daniil@gmail.com"
    gitHubRepositoryOwner = "diklimchuk"
    gitHubRepositoryName = "elmslie"
    gitHubWriteToken = System.env.GITHUB_TOKEN
}
